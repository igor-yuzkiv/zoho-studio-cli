---
type: implementation | fix | refactor | investigation
complexity: S | M | L
model_target: haiku | sonnet | opus
autonomy: { depth: high, decisions: none }
---

# <title>

## Outcome

One sentence: what is true once this is done.

## Context

Where this lives, with real paths. Point, do not paste.

- entry point: `path/to/file.ext:42`
- affected: `path/to/other.ext` — what it does here
- rule in play: … — enforced at `path:line`

## Exemplar

Follow `path/to/existing/file.ext` — same structure, naming, layering, error handling.

Differs in: … (say what should not be copied, and why)

## Changes

What must exist afterwards, per file. Not how to write it.

- `path/…` — what it must do
- `path/…` — what changes and why

## Out of scope

Explicit. What must not be touched, even if it looks adjacent or wrong.

## Decisions made

Settled upstream. Not open for reconsideration by whoever executes this.

- decided: … — because …

## Must not decide

Stop and ask if any of these comes up:

- if … is ambiguous
- if the change would require touching …
- if no existing pattern covers …

## Acceptance

Checkable statements, not intentions.

- [ ] …
- [ ] …

## Verification

Exact commands and expected results.

```
<command from the project profile>
```

Checked by hand: … (what, and what should be seen)

## Provenance

- context from: …
- decisions from: …
- still open: … (and whether it blocks this task)
